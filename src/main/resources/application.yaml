spring:
  main:
    web-application-type: servlet
  application:
    name: ptv-mcp-server

  ai:
    mcp:
      server:
        name: ptv-mcp-server
        version: 0.0.1
        protocol: STREAMABLE

clients:
  ptv:
    baseUrl: https://timetableapi.ptv.vic.gov.au/
    apiVersion: v3